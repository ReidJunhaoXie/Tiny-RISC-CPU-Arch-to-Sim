# 12_指令集編寫

> 以最精簡的 RISC 思想為基礎，設計一組 16-bit 指令集（ISA），配合既有硬體data path，完成從暫存器、RAM、immediate 的基本運算、控制。

> 這裡需要做很多簡化，但這個簡化是指令上的簡化，硬體上還是保留彈性，總體的精神，就是希望兩數運算的格式(MUX固定處理)，可以大幅度減少複雜度

[TOC]

---

## 0. 小小的介紹

終於到了這一步了！可以編ISA了！  
首先，ARM 跟 x86 都是商用的，而且 x86 極為複雜，歸根究柢是 x86 的 ISA 希望硬體支援的事情太多。ARM 身為 RISC 陣營目前的領頭羊，能接觸到的 ISA 資料也相對有限。  
我們要編的指令集是 **RISC** 的精神，但本專案只做最簡單的 RISC。

在系統中，當底層編好指令集，還需要工具做轉換，例如 C 語言透過 gcc tool 轉換成 x86 指令。目前 RISC-V 的最大缺點是**生態**與 ARM、x86 難相提並論。  
綜上所述，這次的RISC會保證功能完整的情況下最簡化指令集。

---

## 1. 指令格式（16 bits）

- **長度**：`16 bits`
- **欄位**：
  - `opcode`：前 `5 bits`（[15:11]）
  - `operand`：後 `11 bits`（[10:0]），作為 **資料地址** 或 **immediate**
- **限制**：目前無 compiler，只能自己模擬

> **注意**：因為**增加了一個暫存器**，整個指令集需要重新考慮data flow。原本的 data flow 是固定的，現在需要考慮 **暫存器-暫存器**、**RAM-暫存器**、**immediate-暫存器** 的path 
> 這也是為什麼只做最簡 RISC：因為非常複雜且耗時。目前硬體底層已完成，因此我們需要用**控制位**來實現這些指令操作；在編指令時，需要仔細考慮每個控制位在該指令下該是 1 還是 0。

---

## 2. 指令規劃（opcode）

| 指令    | 操作碼    | 注釋                                   |
| ------- | --------- | -------------------------------------- |
| halt    | `0b00000` | 終止程式                               |
| ld_a    | `0b00001` | 將記憶體 RAM 中的值載入暫存器 A        |
| ld_b    | `0b00010` | 將記憶體 RAM 中的值載入暫存器 B        |
| save_a  | `0b00011` | 將暫存器 A 的值儲存到記憶體 RAM        |
| save_b  | `0b00100` | 將暫存器 B 的值儲存到記憶體 RAM        |
| add     | `0b00101` | 暫存器 A 與 B 相加，結果存回 A         |
| sub     | `0b00110` | 暫存器 A 減去 B，結果存回 A            |
| and     | `0b00111` | 暫存器 A AND B，結果存回 A             |
| or      | `0b01000` | 暫存器 A OR B，結果存回 A              |
| ld_ia   | `0b01001` | 將 immediate 載入暫存器 A              |
| ld_ib   | `0b01010` | 將 immediate 載入暫存器 B              |
| add_ia  | `0b01011` | 將 immediate 加到暫存器 A              |
| add_ib  | `0b01100` | 將 immediate 加到暫存器 B              |
| sub_ia  | `0b01101` | 從暫存器 A 中減去 immediate            |
| sub_ib  | `0b01110` | 從暫存器 B 中減去 immediate            |
| and_ia  | `0b01111` | 暫存器 A AND immediate，結果存回 A     |
| and_ib  | `0b10000` | 暫存器 B AND immediate，結果存回 B     |
| or_ia   | `0b10001` | 暫存器 A OR immediate，結果存回 A      |
| or_ib   | `0b10010` | 暫存器 B OR immediate，結果存回 B      |
| cmp_a   | `0b10011` | 比較暫存器 A 和 immediate 是否相等     |
| cmp_b   | `0b10100` | 比較暫存器 B 和 immediate 是否相等     |
| jmp     | `0b10101` | 無條件跳轉到指定位址執行               |
| je      | `0b10110` | 若比較結果相等才跳轉                   |
| jne     | `0b10111` | 若比較結果不等才跳轉                   |
| nop     | `0b11000` | 什麼都不做，跳過一條指令               |

> !! 請注意，只是要做CPU科普的到這裡可以了 ! 後面非常繁瑣，==快逃== !

---

## 3. 指令設計的總思路

**邏輯**  
- `Fetch` 與 `Execute` 為兩個 state。`execute state` 下，`en_ir = 0`、`en_pc = 0`；若操作 **immediate**，`en_i = 1`、`ld = 0`
- 建議預設哪個MUX要接什麼類型的資料，如固定MUX_A輸出暫存器，這樣指令會比較一致
- `en_pc` 管 **state**；`ld_pc` 管 **跳轉**
- `en_cmp`：比較控制位；比較時開啟，否則關閉
- `je/jne` 由 `cmp` 結果決定 `ld_pc`

**命名修正**  
- `je_en → ld_pc`  
- `jmp_en → en_cmp`

---

## 4. 每條指令的詳細考慮點

### 4.1 halt
完全與之前一致。

---

### 4.2 `ld_a / ld_b`
- 路徑：`RAM.Dout → S → RegA/RegB`
- 選擇 A 或 B 暫存器時，**另一個暫存器的輸入為 0**
- 簡化問題：將 `MUX_A` 固定接 RAM 的值，`MUX_B` 接 0

<img width="500" height="500" alt="Image" src="https://github.com/user-attachments/assets/ed95108d-9df6-4bcc-99d0-94d49ae88abb" />

---

### 4.3 `str_a/str_b`
- 路徑：`RegA/RegB → RA/RB → ALU.A/B → S → RAM.Din`
- 存 RAM 時，**暫存器的 enable 關閉**；同樣邏輯，`MUX_A` 輸出暫存器資料、`MUX_B` 輸出 0

---

### 4.4 ALU A/B兩數運算：`add / sub / and / or`
- 路徑：`RegA/RegB → ALU → S → RegA`
- 規則：**結果默認存回 A**；`MUX_A`、`MUX_B` 分別負責暫存器 A/B。

---
> !!提醒: 暫存器-immediate兩數的操作原則上是自由的，你可以由MUXA載入RegA到ALU.A，也可以到ALU.B，但為了簡化問題，我們都將邏輯一致，也就是當兩數操作，統一兩個MUX的行為，暫存器的值就是走MUX_A，immediate就是走MUX_B，減少開發的負擔
> 
### 4.5 暫存器 - immediate：`ld_ia / ld_ib`
- 路徑：`en_i=1 → operand → Dout → (MUX) → ALU.A/B → S → RegA/RegB`
- 原則：固定讓 `MUX_A` 負責 **immediate**、`MUX_B` 輸出 **0**。

<img width="500" height="380" alt="Image" src="https://github.com/user-attachments/assets/39f27dd1-9cad-49ae-ad9e-b3f10bd76d04" />

---

### 4.6 暫存器與 immediate 運算：`add_ia/ib`, `sub_ia/ib`, `and_ia/ib`, `or_ia/ib`
- 原則：
  - **凡操作 immediate，不需要 RAM** 的資料，`ld = 0`。
  - 以 `add_ia` 為例：暫存器 A 與 immediate 相加，immediate 視作 **B 端**，因此 `sel_b` 選 `Dout`
  - 同樣邏輯 : `MUX_A`負責暫存器；`MUX_B`負責immediate
- `sub/and/or`只需要改 ALU 的 `sel`，其他資料流一致

---

### 4.7 比較：`cmp_a / cmp_b`
- 「比較」在幾個章節前的作法是 always-on，直到加上en_cmp控制，並且實際寫程式需要的是A跟immediate比較
  - 例：`RegA` 與 `Immediate` 比較，需要把 immediate 放到 `ALU.B`。
  - data path：`RAM.Dout(指令) → operand(IR) → en_i=1 → Dout(immediate) → ALU.B`
- 比較**不應該改變暫存器**：將 `en_a/en_b` 關閉。  
- `MUX_A` 固定 **暫存器**，`MUX_B` 固定 **immediate**。

---

### 4.8 跳轉：`jmp / je / jne`
- `jmp/je` 與之前一致，需要跳轉時 `ld_pc = 1`
- `jne` 與 `je` 差在判斷條件相反
- `en_cmp` 在較早版本是 always-on，比較持續進行；目前當成**比較控制位**，當比較類指令時設為 1
- **跳轉與計算無關**：`MUX_A`、`MUX_B` 全為0

---

### 4.9 `nop`
- 只要 `halt=1` 其他控制位全為0

---

### 4.10 編指令技巧提示

1. **先判斷 state**：此處只編 `execute state`。在此狀態下 `en_ir = 0`、`en_pc = 0`
2. **操作 immediate**：`en_i = 1`、`ld = 0`
3. **跳轉類**：只看 `ld_pc`；`en_pc` 管 state，`ld_pc` 管是否跳轉
4. **cmp=0**：最後再針對 `cmp=1` 調整`je/jne` ，兩者相反

> **提醒**：`je_en → ld_pc`；`jmp_en → en_cmp`

---

## 5. LUT 、 opcode

- 先給每個指令 `opcode` 順排
- 將指令控制位編碼(就是machine code)填入 **LUT**，在實際電路行為上等同於**燒錄**

<img width="500" height="300" alt="Image" src="https://github.com/user-attachments/assets/18dec3ac-5a74-41c3-8d5c-6b44cfa45649" />


---

## 6. 實作＆模擬
### 7.1 程式規劃
| time | instruction | Instruction Description  | opcode | operand或immediate | data(操作值) | whole instruction  |                   |
|------|-------------|--------------------------|--------|-------------------|-----------|--------------------|-------------------|
| 0    | Fetch       | Fetch Instruction to IR  |        |                   |           | opcode+operand     |                   |
| 1    | ld_ia       | load immediate to reg_A  | 01001  | 00000010011       | 19        | 0b0100100000010011 | 19->Reg_A         |
| 2    | Fetch       | Fetch Instruction to IR  |        |                   |           | 0b                 |                   |
| 3    | ld_ib       | load immediate to reg_B  | 01010  | 00000101101       | 45        | 0b0101000000101101 | 45->Reg_B         |
| 4    | Fetch       | Fetch Instruction to IR  |        |                   |           |                    |                   |
| 5    | add         | ALU A+B                  | 00101  | 00000000000       | x         | 0b0010100000000000 | A+B->A            |
| 6    | Fetch       | Fetch Instruction to IR  |        |                   |           |                    |                   |
| 7    | str_a       | strore value of A to RAM | 00011  | 00000010001       | 地址(17)    | 0b0001100000010001 | store to addr(17) |
| 8    | Fetch       | Fetch Instruction to IR  |        |                   |           |                    |                   |


### 7.2 準備 RAM（在模擬器裡面編輯)
現在來寫個程式做模擬，**編輯 RAM**：

<img width="600" height="200" alt="Image" src="https://github.com/user-attachments/assets/4be6a1ec-dddd-4415-95d8-c0c4b7683ab7" />

---

### 7.3 模擬

#### 剛上電 - 模擬開始
- `clock = 0`
- **Fetch state**
- 初始化時，IR 內為 `0`，因此 `input code (op + cmp_q + state) = 0b0000000`，電路行為：`halt=1, en_ir=1, ld=1, en_pc=1`，其他為 0。
- `PC = 0`，且 `en_pc = 1`，指向 RAM 第一條指令：`0x4813`  
  
  <img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/d435bdd3-13b8-4230-9a1d-8d938a55c195" />

---

#### clock = 1
- **Execute state**
- 指令 `0x4813` 進入 IR，分解為 `operand + opcode`，此時執行 `ld_ia`；`operand` 高位補 0 成 `immediate (D_out)` 指向 `MUX_A`。  
- 數值 `19` 目前在 `ALU.S`，將於下一個 clock 進入暫存器。  

<img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/f3772c09-1178-4eef-abf9-8278e70587df" />

---

#### clock = 2
- **Fetch state**
- `PC = 1`，指向 RAM 第二個位置。
- 指令 `0x502D` 在 RAM 輸出端。
- 此時 IR 仍為上一指令，但 **Fetch 行為一致**：`halt=1, en_ir=1, ld=1, en_pc=1`，其他為 0。  

<img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/3e7926c4-e8ea-4ef0-a50e-db136b150cd0" />

---

#### clock = 3
- **Execute state**
- 指令 `0x502D` 進入 IR，分解為 `operand + opcode`，執行 `ld_ib`；`operand` 高位補 0 成 `immediate (D_out)` 指向 `MUX_A`。  
- 數值 `45` 目前在 `ALU.S`，將於下一個 clock 進入暫存器。  

<img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/eea23160-80a0-47a4-a690-842290dc145b" />

---

#### clock = 4
- **Fetch state**
- `PC = 2`，指向 RAM 的第三個位置。
- 指令 `0x2800` 在 RAM 輸出端。
- **Fetch 行為一致**：`halt=1, en_ir=1, ld=1, en_pc=1`，其他為 0。  

<img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/28ff2507-e8ac-4a3a-9ac9-108fb673a34e" />

---

#### clock = 5
- **Execute state**
- 指令 `0x2800` 進入 IR，執行 `add`，ALU 兩數相加。  

<img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/2d7c01b8-9ba1-4c54-8bc0-9b93f40325a2" />

---

#### clock = 6
- **Fetch state**
- `PC = 3`
- 指令 `0x1811` 在 RAM 輸出端。
- **Fetch 行為一致**：`halt=1, en_ir=1, ld=1, en_pc=1`，其他為 0。  
  
  <img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/6f2bf38c-b8a6-4c3d-8d34-0b7fdc0c374d" />

---

#### clock = 7
- **Execute state**
- 指令 `0x1811` 進入 IR，分解為 `operand + opcode`，執行 `save_a`（原 `str_a`），`operand` 指向要寫入的 RAM 位址 `17`。  

<img width="800" height="300" alt="Image" src="https://github.com/user-attachments/assets/7daf8a95-56fd-477e-ad07-04d7e36b12c6" />

<img width="800" height="250" alt="Image" src="https://github.com/user-attachments/assets/402a2876-bf0d-4dbb-9d95-6b0271d5ab44" />

---

## 8. 提醒

- 先把 `cmp=0` 的情況全部編好，再補 `cmp=1` 的跳轉路徑（`je/jne` 互為相反）。  
- 記得「簡化原則」：  
  - 暫存器經 `MUX_A`，immediate 經 `MUX_B`。  
  - RAM 只在需要時開 `ld`；操作 immediate 時關閉。  
  - 結果默認回寫至 `RegA`。  
  - 跳轉由 `ld_pc` 觸發；比較由 `en_cmp` 管控。

