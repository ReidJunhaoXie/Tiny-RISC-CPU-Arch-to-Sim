# 13_組合語言模擬
這次是真的要嘗試寫程式了!正常的程式實際上是: 高階語言(C)->組合語言->machine code，現在，實現下面簡單的C code，從組語->machine code自己編譯

## C code -> 組合語言
C dode 如下 :
```
int main()
{
    int S=0 ;
    for(int i=1;i!=101;i++)
    {
        S+=1 ;
    }
}
```
這段C code對應的組合語言，參考以下網址做compiler轉換:
https://gcc.godbolt.org/

```
main:
        push    rbp
        mov     rbp, rsp
        mov     DWORD PTR [rbp-4], 0
        mov     DWORD PTR [rbp-8], 0
        jmp     .L2
.L3:
        add     DWORD PTR [rbp-4], 1
        add     DWORD PTR [rbp-8], 1
.L2:
        cmp     DWORD PTR [rbp-8], 100
        jne     .L3
        mov     eax, 0
        pop     rbp
        ret
```

### 組語的轉換

首先，這段程式需要兩個變數:
> 變數對應：S → 暫存器 A，i → 暫存器 B
1. 暫存器A賦值0 
2. 暫存器B賦值1
3. jmp->6 : 先比較B是否等於101，如果相等迴圈直接結束
4. A <- A + B   -<
5. B <- B + 1   -<  
6. 判斷B是否等於101
7. If 不相等 : jne->3 ,then : jne->迴圈結束
8. 把結果A保存在RAM的某個位置
9. 程式結束

組語
0. ld_ia 0
1. ld_ib 1
2. jmp 5
3. add 0
4. add_ib 1
5. cmp_b 101
6. jne 3
7. str_a 16
8. halt

> 兩者整合

| 步驟 | 流程說明                                                                 | 組語（行號: 指令）     |
| ---- | ------- | ------- |
| 1    | 暫存器 A 賦值 0                                                           | 0: `ld_ia 0`            |
| 2    | 暫存器 B 賦值 1                                                           | 1: `ld_ib 1`            |
| 3    | `jmp->6`：先比較 B 是否等於 101，若相等迴圈直接結束                      | 2: `jmp 5`（跳到行 5）  |
| 4    | `A ← A + B`                                                               | 3: `add 0`              |
| 5    | `B ← B + 1`                                                               | 4: `add_ib 1`           |
| 6    | 判斷 B 是否等於 101                                                       | 5: `cmp_b 101`          |
| 7    | 若不相等：`jne->3`；否則：迴圈結束                                        | 6: `jne 3`              |
| 8    | 把結果 A 保存在 RAM 的某個位置                                            | 7: `str_a 16`           |
| 9    | 程式結束                                                                  | 8: `halt`               |

## 組語->Machine Code

下一步就是人工組譯成machine code，燒錄到RAM，machine code一共16bits: opcode(5bits) + operand(11bits)
把指令對應的opcode跟對應的operand填進去就好

| 組語                | machine code <- opcode+operand  |
|-----------------   |---------|
| 0: ld_ia  0        | 0b0100100000000000 |
| 1: ld_ib  1        | 0b0101000000000001 |
| 2: jmp    5        | 0b1010100000000101 |
| 3: add    0        | 0b0010100000000001 |
| 4: add_ib 1        | 0b0110000000000001 |
| 5: cmp_b  101      | 0b1010000001100101 |
| 6: jne    3        | 0b1011100000000011 |
| 7: str_a  16       | 0b0001100000010000 |
| 8: halt            | 0b0000000000000000 |

<img width="700" height="220" alt="Image" src="https://github.com/user-attachments/assets/319d0f9b-930d-4438-a277-df7e0e41f619" />

## 模擬

一切準備就緒，開始模擬，這次沒辦法一步一步地去走過一遍data path，設定clock頻率為100，結果如下

<img width="700" height="300" alt="Image" src="https://github.com/user-attachments/assets/f21211a8-32a3-4aef-ab98-ed9a277b165d" />

RB裡面存的就是正確答案5050 !

<img width="700" height="300" alt="Image" src="https://github.com/user-attachments/assets/8f006369-5df9-4f72-86c1-cdaaf1f30daf" />

# 到目前為止CPU本體已經大功告成 ! 